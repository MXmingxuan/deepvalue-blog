{
  "name": "deepvalue-blog",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "sync": "rmdir /s /q public 2>nul & npm run build && xcopy /s /e /i dist public"
  },
  "dependencies": {
    "astro": "^6.1.6"
  }
}
