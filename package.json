{
  "name": "deepvalue-blog",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "sync": "npm run build && rm -rf public && cp -r dist public"
  },
  "dependencies": {
    "astro": "^6.1.6"
  }
}
